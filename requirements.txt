# Core dependencies for media-sorter
# These are the same as pyproject.toml [project.dependencies]
# Use flexible version ranges for compatibility with other packages

PyExifTool>=0.5.6
imohash>=1.1.0
tqdm>=4.66.0
